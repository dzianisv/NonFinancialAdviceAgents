Moved. The blind pairwise eval protocol (rubric authoring, judge prompt, tally, upgrades, history) now
lives at `.agents/skills/blind-pairwise-eval/SKILL.md` — it was generalized beyond stock hierarchies to
any generator variant (hierarchy, workflow, prompt version). Read it there.
