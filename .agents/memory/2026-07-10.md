## Point Wild "missing accepted equity" — RESOLVED (CS-223 was it all along)

- User pasted the Carta cert detail: **CS-223 (1,798 sh) was created from the repurchase of CS-103**.
  The "liquidity event" = issuer repurchase of 492 of the 2,290 exercised shares; the accepted equity
  the user was looking for IS CS-223, already on the Equity tab. No separate/hidden position exists.
- Cost basis carried over from the original 6/22/2025 exercise: **$1.31/sh, $2,355.38 total** (Carta
  had shown $0.00 on the portfolio summary — the detail page is the ground truth).
- Sheet update (subagent, verified): Equity row 15 F=1.31 + repurchase note in J; H15 `=G15*D15`
  = $17,980 untouched; `Equity!L1` = $55,019.51 and `Totals!C10` unchanged; `Totals!C11` ≈ $2,002,993
  (±$39 vs prior read = live GOOGLEFINANCE drift, not this edit).
- Carried lesson from 07-08 not yet logged: orphaned **Table objects enforce column types over cell
  formats**, and `deleteTable` **wipes cell contents** under the table footprint — delete, then rewrite
  data + formats. Also: Point Wild cert cancellations were issuer-side (ES-130 repricing→ES-897;
  CS-138 mirrors Aura CS-144 reorg 10/22/2025).

## DeFi tab refresh — scam-jetton fix verified live (crypto-portfolio skill)

- Reran full pipeline (`defi_positions.py`) + DeBank browser cross-check (L1/L3/B1/B3/B5) +
  wrote merged view into DeFi tab of `1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8` via `gws`.
- **Grand total $179,503.30** across L1 $12,139.48 / L3 $92,394.75 / B1 $321.10 / B3 $26,683.44 /
  B5 $4.17 / T1 $22,238.93 / SOL.L1 $25,719.38 / SOL.2 $2.05. `DeFi!I1`=179503.3, `H1`="GRAND TOTAL:".
- **Confirmed PASS**: the 9 TON scam/spam jettons (DOFA, GRAM Unlock x2, GRAM AT GRAMEVENT.ORG,
  TONRAGE, $BLUM x2, SWPG, Unfreeze) are gone from T1 and the whole tab; legit T1 DeFi rows intact
  (USDT Storm LP $9,004.24, TON Storm LP $6,941.73, DeDust Pool TON/USDT $6,173.69, YT USDT-SLP and
  Coffee DEX TON/USDT LP kept visible at $0 as designed, not scam).
- **New coverage gap logged**: Lighter on L3 ≈ $593 (Perps Available + Spot LIT + SOL/USDC perp),
  DeBank-only, no pipeline module yet — added as 3 manual rows this run. Lighter on B3 negligible
  ($0.00001, not added). Save/Solend on SOL.L1 carried forward at $16,647.74 (not re-fetched this
  run — still no automated read path, per skill's Known gaps).
- **Sheet-write gotcha found+fixed**: Sheets API `values.update` doesn't clear cells beyond a
  shorter row's array length — writing new 7-col TOTAL rows over old longer rows left 2 stale
  Note-column leftovers (H78, H90) from the pre-cleanup table. Fixed by explicitly blanking that
  range. Lesson for future DeFi-tab writes: after any row-count-shrinking update, explicitly clear
  the old range width, don't rely on partial overwrite.

## T1 $0-value DeFi positions fixed — Coffee LP priced, Storm YT confirmed worthless

Root cause was NOT the scam filter (that's a separate, already-shipped fix) — these two are real
positions swap.coffee's public jetton-price feed has no `market_stats` for at all, verified via
live curl on both tokens.swap.coffee and TONAPI (both return price=0/absent).

- **"Coffee DEX: TON/USDT LP"** (`0:c44cf0b6...9c08ea`): the pool contract itself IS the LP jetton
  master on swap.coffee (confirmed via TONAPI `interfaces=["coffee_pool","jetton_master"]`).
  `GET backend.swap.coffee/v1/dex/pools` lists every pool by that same address (friendly/base64
  form) with human-readable `reserves` for `tokens` (`"native"`=TON, else priced via TONAPI
  `/v2/rates`). LP fair value = pool TVL / total LP supply (supply from TONAPI
  `/v2/jettons/{addr}`). Implemented pure-Python raw→friendly TON address conversion (CRC16/XMODEM,
  no external TON lib) in `ton_positions.py::_raw_to_friendly`, verified against
  `tonapi.io/v2/address/{addr}/parse`. Result: **$1,826.56** (19.926532 LP tokens, pool = 575.07 TON
  + 961.24 USDT, at TON≈$1.676).
- **"YT USDT-SLP (01Sep2025)"** (`0:ec353180...314f71`): NOT a Storm Trade token — it's a **FIVA
  protocol** (thefiva.com) Pendle-style yield token minted against Storm SLP (found via the jetton's
  image CDN path referencing `Fiva-protocol/jettons-manifest` on GitHub). FIVA's own API
  `GET api2.thefiva.com/protocol_metrics` matches this exact jetton to `service="stormtrade_usdt"`,
  `maturity_date="2025-09-01"`. FIVA's own FAQ states outright: *"Maturity is ... the date until
  which you farm points and receive yield from YT. After that date YT is worth 0."* Priced **$0**,
  now with a citation note instead of a silent gap — genuinely verified (protocol's own maturity
  mechanics + live API confirming this specific market is matured), not assumed from the name.
- Code: `.agents/skills/crypto-portfolio/scripts/ton_positions.py` — added `_raw_to_friendly`,
  `_coffee_pools`/`_coffee_lp_value_usd`, `_fiva_metrics`/`_fiva_yt_status`, wired into the
  no-price branch of `fetch_positions()`. Tested standalone against T1, then full pipeline rerun,
  then `DeFi!G76:H77` written via `gws` (T1 TOTAL/GRAND TOTAL are `=SUM()` formulas so they
  auto-recomputed to $24,065.49 / $181,329.86); `DeFi!I1` mirror updated to match. Verified live via
  `gws sheets spreadsheets values get`. Not committed — left for owner review per instruction.

## DeFi book review (user ask: cut noise, consolidate, align risk) — via gws CLI read of DeFi tab
- Sheet snapshot 2026-07-10, GRAND TOTAL $181,330 across 8 wallets (sheet fresher than .cache CSV: adds Lighter on L3 ~$593 and SOL.L1 Save USDC lending $16.6k).
- Mix: ~80% stables (of which ~$52.8k sits IDLE at 0% — L3 USDC $44.6k, L1 USDC $4.3k, SOL.L1 USDC $3.0k, HL withdrawable $0.8k), ~20% beta (HYPE $12.9k, TON ~$11k, SOL ~$5.5k, ETH ~$3k, tail ~$2.5k).
- Noise: 3 dust wallets (B1 $321, B5 $4, SOL.2 $2), ~25 token-dust lines <$50, matured FIVA YT ($0), $18 Uniswap V3 LP, 3 leveraged perp longs (ASTER 3x, JUP 10x, Lighter SOL 2x) with combined P&L −$1,207 vs ~$650 remaining margin.
- Duplication: Maple Syrup USDC held in BOTH L3 ($9.2k) and B3 ($8.5k); Hyperliquid venue exposure across L1+L3 ≈ $20k (11%).
- TON Storm LP valuation divergence: TON-SLP swap.coffee $1.5047 vs TONAPI $2.1378 (−42%); USDT-SLP 8% divergence. T1 $24k book has weak thesis (TON = HOLD, insufficient history).
- Recommended end-state: 8 wallets → 3 (L3 = EVM main, SOL.L1, T1 wind-down), ≤6 protocols, ~15 lines; deploy idle $52.8k (yield ≈ +$3–4k/yr or the 07-09 governor BUYs ETH/SOL/AAVE/UNI).

## Full DeFi portfolio accounting audit (user ask: "double check quantity/price correct, balance not 0, be careful, important accounting")

- Ran 3 parallel independent re-verification agents, one per chain group (EVM via live DeBank +
  Hyperliquid info API; TON via TONAPI/swap.coffee/DeDust/FIVA cross-checks; Solana via raw RPC +
  independent price sources), each blind to the others, each told to distrust the pipeline's own
  output and re-derive numbers from a different source.
- **Result: every existing balance/price in the pipeline (all ~70 rows across 8 wallets, plus the
  scam-jetton and Coffee-DEX-LP/FIVA-YT fixes shipped earlier today) came back CONFIRMED.** No
  decimals bugs, no stale prices, no double-counting anywhere. The audit earned its keep by finding
  2 real gaps instead:
  1. **L1 UPUMP token, ~$644, silently zeroed** by `hyperliquid_positions.py`'s old blanket
     "no perp oracle -> $0" rule (a real, moderately-liquid HL spot holding, not dead dust like MAX).
     Fixed with a spot-mark fallback. First implementation had a real bug caught by testing against
     live balances before shipping: HL's `spotMetaAndAssetCtxs` ctxs array is indexed by each pair's
     `index` field, NOT its position in `universe` — zipping by enumeration order silently priced
     UPUMP off the wrong pair ($46,800!) and MAX off another ($3.9M!). Fixed by indexing
     `ctxs[pair["index"]]`; both now match: UPUMP $644.84, MAX $0.66 (correctly still dust-filtered
     on L1 at $0.29 baseline, kept on L3 at $0.66 since it's just above the $0.50 floor there).
  2. **SOL.L1 Save/Solend deposit, ~$16,648, was a manual carry-forward number with zero automated
     coverage** (flagged as a known gap 2026-07-09, never wired up). Solana audit agent found and
     validated the read path (getProgramAccounts memcmp on Obligation.owner @ offset 42, decode
     against solend-sdk's byte layout, price live off api.solend.fi/v1/reserves' cTokenExchangeRate
     + oracle price — NOT the obligation's own cached, often-stale marketValue field). Had it hand
     over the exact code rather than re-deriving the byte offsets myself (too easy to introduce a
     new decode bug in exactly the kind of task this audit was meant to catch). New standing module:
     `solend_positions.py`, wired unconditionally into `defi_positions.py` for every solana wallet.
     Verified: $16,648.23 live vs $16,647.74 prior manual figure — the ~$0.50 gap is ~2 weeks of
     accrued interest, not error.
- **New open question, not resolved**: FIVA's matured YT token is correctly priced $0 (re-confirmed
  independently), but FIVA's own docs describe a SEPARATE manual-claim mechanism for pre-maturity
  accrued yield ("claim via the FIVA Dashboard at any time") that isn't visible via any public REST
  endpoint tried (`api2.thefiva.com/{user,users,rewards,claimable,account}/<addr>` all 404'd). If the
  owner never manually claimed yield before the 2025-09-01 maturity, there could be real unclaimed
  value this pipeline structurally cannot see (requires a wallet-connected `app.thefiva.com` check).
  Flagged in SKILL.md "Known gaps", not guessed at either direction.
- **Lighter (~$595 on L3, negligible <$0.01 on B3)**: still NOT automated despite finding Lighter has
  a public API (`mainnet.zklighter.elliot.ai/api/v1/account?by=l1_address&value=<addr>`, no key) —
  its cross-margin fields (`available_balance` vs `collateral` vs per-position
  `cross_initial_margin_requirement`) don't unambiguously triangulate to a single "how much is really
  the investor's money" total from the raw API alone; a wrong auto-computed number would be worse
  than a manual DeBank-verified one for a $595 line item. Left as a documented manual line, added to
  the sheet with a MANUAL note each time until the accounting model is actually understood.
- Code shipped (commit `3cdb37a`, pushed to origin/main): `hyperliquid_positions.py` (spot-mark
  fallback fix), `solend_positions.py` (new), `defi_positions.py` (wiring), `SKILL.md` (rules 4/6
  updated, Known gaps / new Resolved gaps sections, source map, compatibility deps: `base58` +
  Solana RPC + api.solend.fi).
- Post-fix pipeline GRAND TOTAL (automated only): $181,357.63. Full sheet total incl. manual Lighter
  lines: **$181,949.18** (up from $181,330 pre-audit — the delta is coverage gained, not portfolio
  growth: +$644 UPUMP, +$16,648 Solend now automated, minus the previous $584→$595 Lighter refresh
  and normal intraday price drift across TON/HL positions).
- DeFi review report published to Notion under crypto-advisor page: "🧹 DeFi Book Review — Consolidation & Risk Alignment — 2026-07-10" (https://app.notion.com/p/399ac25eb49f81e6a6eceef2c7fd9d30). Open decisions: idle-cash split, TON wind-down, HL venue cap.
