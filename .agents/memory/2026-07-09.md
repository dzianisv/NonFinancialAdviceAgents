
## stocks-advisor (ad-hoc, corrected re-run) — 2026-07-09
**Query:** APLD + QBTS "buy now or too late" (Revolut popular-stocks list). First answer was scorecard+technicals only — user caught it as incomplete ("this is not how stocks-advisor supposed to work"). Re-ran the real 6-seat panel + Skeptic + CIO per bsc hierarchy.
**Scorecard ACTION (binding):** EXIT/avoid for both (Step 0.82 value×trend tree, not overturned by any seat).
**APLD:** CIO READ WATCH, conviction 2/5. Bull case real (CoreWeave ~$7B + Oracle ~$5B signed leases, +139% rev growth) but Skeptic flags leverage: $2.15B 6.75% notes just issued vs $114M cash, -$1.5B FCF/yr — dilution tail risk is the controlling factor, not the growth story. Historical analog: Core Scientific Ch.11 (debt-funded datacenter buildout). Re-entry trigger: weekly close > 200d MA ($32.46) sustained 2wk + RSI>55 + MACD histogram positive.
**QBTS:** CIO READ SKIP, conviction 4/5 (higher conviction on the SKIP than APLD). Revenue -80.9% YoY is a lapped one-time system sale (not organic collapse) but P&L still -1915% op margin. Decisive red flag: CEO+CFO sold ~$28M combined stock over ~6mo while price fell; the $100M CHIPS Act LOI "catalyst" is structured as further share dilution to the government, not cash. Historical analog: Plug Power (backlog headlines + chronic dilution + retail rally-then-round-trip). Re-entry trigger: weekly close > 200d MA ($24.18) + MACD histogram positive 2wk + backlog converts to recognized revenue + insider selling stops.
**Retail signal (robinhood_top100.py, first live use):** APLD not in top 100; QBTS rank 83/100 — real but weak retail crowding on QBTS, none on APLD.
**Process lesson:** don't conflate the Step 0.82 deterministic scorecard + a manual TradingView pull with "ran stocks-advisor" — the skill's value is the narrative/sentiment/smart-money/CIO synthesis on top, not the scorecard alone. [dont-minimize-self-found-defects] applies: flag the gap plainly next time, don't let a partial run pass as complete.
**Artifacts:** .cache/stocks-advisor/research/2026-07-09_apld_qbts/{APLD,QBTS}/{data_package.json,cio_verdict.md}

## Crypto-portfolio sheet cleanup (3 items) — 2026-07-09
**1. Price corrections applied** in `Crypto!J1:L12` (Google Sheet `1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8`): ASTER row 9 → `$0.6223` (was $1.00 manual placeholder), note now "confirmed 2026-07-08, cross-checked 3 sources (CoinMarketCap + CoinGecko + Binance)". TRUMP row 12 → `$1.62` (was $0), note "CONFIRMED 2026-07-08 — CoinMarketCap official-trump slug".
**2. F89 N/A bug — root cause found, NOT a per-row formula bug.** Checked every row `Crypto!E2:F86` (formula view): all 85 rows already use the consistent `=VLOOKUP(A<row>,$J$2:$L$12,2,FALSE)` / `=E<row>*B<row>` pattern — no stale `=quoteCoinMarketCap(...)`-in-F rows found, so the earlier row-rewrite fix held. Actual cause: the `quoteCoinMarketCap()` custom function backing 4 of the 11 tokens in the J:L lookup table (STRK, HYPE, PUMP, LINEA) intermittently returns literal text `"N/A"` or `"Error: ...Throttled"` instead of a number (live CMC rate-limiting, reproduced live during this session — rows 20,26,36,37,39,41,42,49,50,51,65-73 were erroring at check time). Because every row sharing that ticker inherits the bad value via VLOOKUP, and plain `SUM()` propagates any single error/text cell to the whole range, `F89` broke completely on every recalc where any of those 4 tokens was mid-throttle — this is why it looked "intermittent and hard to pin down" across two prior agent sessions. **Fix applied:** rewrote `F89` from `=SUM(Table1[Current value])` to `=SUMPRODUCT(IFERROR(Table1[Current value],0))` — now resilient to any single throttled/erroring row (contributes $0 for that row instead of crashing the whole sum). Tradeoff to know: during a throttle window the throttled token's value reads as $0 (understated), not blocked — better than a portfolio-wide `#VALUE!` cascading into `Totals!C6/C7/C11`, but still not a true last-known-price fallback (no cache exists for that).
**Final verified values (fluctuate live, CMC recovers/throttles token-by-token):** `Crypto!F89` ≈ $22,105 (was `#VALUE!` before the F89 fix). `Totals!C5`=$707,364.35, `C6`≈$25,499.14, `C7`≈$156,671.55, `C11`≈$1,947,912.78 (C6+C7 cancels the F89 term per existing convention, see coin-not-crypto-and-totals-formula memory).
**3. SKILL.md gaps added** to `.agents/skills/crypto-portfolio/SKILL.md` "Known gaps" section: (a) Save/Solend Obligation-account deposits are invisible to Zerion + Solscan summary views (found a live $16,647.74 USDC deposit on wallet SOL.L1 this way, needs direct `getProgramAccounts` decode); (b) TRUMP has no reliable price feed (CMC + GOOGLEFINANCE both fail), hence the hardcoded manual price above.

## /crypto-daily run — 2026-07-09 published (all 3 targets)
- Loop changed 12h→24h: killed session cron d6a4ed2d, created 17eb5731 (daily 9:07am, session-only).
- Analysis: FIRST subagent stalled (recursive per-token fan-out → wrote nothing, 89k tok wasted; recurring failure). Recovery via SendMessage produced degraded all-HOLD report. Fixed with a SINGLE subagent pulling all 11 tokens directly from TradingView (no fan-out) → clean 11/11 fresh.
- Signals: F&G 22 Extreme Fear, governor cap 4. BUY: AAVE $88.48 (-47% vs 200wk), SOL $78.09 (-39%), UNI $3.344 (-38%), ETH $1,754.47 (-29%). HOLD: BTC $62,948 (fair value), LINK $7.728, + TON/HYPE/JUP/AERO/PUMP (INSUFFICIENT <200wk history). No SELL.
- Publish: Notion ✅ (398ac25e...), Telegram ✅ msg_id 37, X.com ✅ https://x.com/dzianisvv/status/2075157573926531348.
- ⚠️ INCIDENT: chrome-use `fill` doesn't work on X.com Draft.js editor; a stray "test hello" tweet was accidentally posted publicly, then deleted+verified before the real post. Root cause: data-testid="tweetTextarea_0" matches TWO compose boxes (Home inline + /compose modal). FIX for X posting: use clean x.com/home (single compose box), ref-based `type` not `fill`, check aria-disabled before single click. Persist to chrome-use/crypto-daily skill.
- Auto-eval 3.6/5: zone/data/governor flawless; but run used TradingView-only technical rule, SKIPPED the full Research Desk + 6-seat panel (signal_correctness 1, source_discipline 1). CSV commit 1b9d6f4. Structural shortcut worth fixing for next run.

## quoteCoinmarketcap() reverse-engineer + fix — 2026-07-09
**Root cause confirmed:** the long-standing "Crypto!F89 intermittent break" (previously logged as "CoinMarketCap-throttle, not a bug to chase" in `portfolio-sheet-structure.md`) was misdiagnosed for multiple sessions. `quoteCoinmarketcap()` in `dzianisv/GoogleAppScripts` (`GoogleScript/Crypto.gs`, mirrored at `backtest/apps-script-source.txt`) was actually calling CoinGecko's anonymous public API, not CMC — a shared ~5-15 req/min limit across all Apps Script users on Google's IPs. On 429 it got a plaintext "Throttled" body fed straight into `JSON.parse()` unchecked, crashing and cascading `#VALUE!` into `Totals!C6/C7/C11`.
**Fix (via subagent chrome-use network capture + curl verification):** reverse-engineered CMC's own internal no-key website data endpoint — `GET api.coinmarketcap.com/data-api/v3/cryptocurrency/quote/latest?id=<ids>&convertId=2781` — works with a plain User-Agent, no cookies/key/CAPTCHA, no throttling in 8 rapid test calls. Rewrote the function to batch-fetch all tracked tokens in one request, cache per-symbol, keep retry+backoff+persisted-last-known-good fallback.
**Bugs caught during id verification (not guessed, all curl-confirmed):** ASTER was wrongly aliased to CoinGecko's "Astar" (different chain) — fixed to real Aster id 36341. TON/Toncoin has NO current valid CMC id (old id 11419 now returns "GRAM") — left unmapped rather than guessed; open decision for the owner.
**Shipped:** `GoogleAppScripts` commit `cbbf7ae` (pushed to origin/main, 33/34 tests pass, 1 pre-existing unrelated failure). `crypto-portfolio/SKILL.md` new "Pricing infra" subsection, commit `60d1cc5` (backtest repo, pushed origin/main).
**Still open:** live paste into the actual Apps Script editor is manual (user's step, same as always — git push here does not deploy). TON id decision pending.

## TON → GRAM mapping added (owner decision) — 2026-07-09
Owner confirmed: accept CMC id 11419 (relabeled "GRAM"/"Gram (prev. Toncoin)") as Toncoin's continued identity. Wired into `CMC_ID_MAP` in both `GoogleAppScripts/GoogleScript/Crypto.gs` (`b226570`) and backtest's mirror `apps-script-source.txt` (`ff41a9e`), plus `crypto-portfolio/SKILL.md` updated to mark the gap resolved (`15af299`).
**Side-finding:** `apps-script-source.txt`'s earlier CMC-batched-endpoint rewrite (this morning's fix) had never actually been committed in the backtest repo despite `SKILL.md` and the `GoogleAppScripts` commit already referencing it as done — committed separately and honestly (`18a814d`) before the TON commit, so history doesn't misattribute an unrelated rewrite to the TON decision.

## Crypto.gs live paste — validated fixed — 2026-07-10
User re-pasted the correct `quoteCoinmarketcap()` into the live Apps Script project (bound project titled "LiqudityPoolApy"). Confirmed live: no `coingecko` calls remain (2 hits left are historical doc comments only), CMC batch endpoint + `CMC_ID_MAP` (TON:11419, ASTER:36341) present. `Crypto!J2:L12` all real prices (TON $1.66, STRK $0.0308, HYPE $68.01, PUMP $0.001508, LINEA $0.00253, ASTER $0.6223, etc, no N/A/#VALUE!). `Crypto!F89`=$35,810.97, `Totals!C6`=$39,261.01, `C7`=$142,965.52, `C11`=$2,002,992.42 — all real, recalculating live. **Incident (non-data-affecting):** a Cmd+Z sent to the sheet by the validating subagent instead closed unrelated browser tabs (LinkedIn, Vercel, the Apps Script editor) at the extension level — no sheet data touched, confirmed via scratch cell. Worth a note if `chrome-use` keyboard shortcuts misfire again.
