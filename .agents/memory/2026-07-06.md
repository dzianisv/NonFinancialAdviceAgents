
## crypto-daily cron fixed (was silently broken since setup)
- User ran `/loop 12h /crypto-daily`; chose durable local execution (Telegram+X need live local Chrome/session, can't run in cloud).
- Existing crontab entry `copilot "run /crypto-daily"` was INVALID — copilot non-interactive needs `-p/--prompt` + `--allow-all`. Log at /tmp/crypto-daily.log showed only "Invalid command format" errors → the daily 9am job had NEVER run successfully.
- Fixed to: `0 9,21 * * * copilot -p "run /crypto-daily" --allow-all >> /tmp/crypto-daily.log 2>&1` (every 12h, 9am+9pm PT).
- Smoke-tested `copilot -p "..." --allow-all` → exit 0. Lesson: always verify the runner, not just the schedule. Cron only fires while Mac awake (no wake-catchup; launchd would).

### Runner corrected to claude (not copilot)
- User asked to schedule "as claude"; the stale crontab used copilot — wrong runner, inherited by mistake.
- Switched to stable binary `/Users/engineer/.local/bin/claude -p "run /crypto-daily" --dangerously-skip-permissions` (PATH `claude` is an ephemeral cmux shim — unusable in cron).
- Smoke-tested headless claude → exit 0. CAVEAT: supervisor stop-hook (on-stop.mjs) fires inside nested -p runs and injects "goal" nagging; it still exits 0 but may add noise/token cost. Consider `supervisor:off` or a hook-disabled settings profile for unattended runs.

## intraday-bot: regime_sma_maker fresh-symbol falsification test — FAILED CONFIRMATION (2026-07-06)

**Pre-registered, zero-parameter-change confirmation run** per ROADMAP.md Section 5 ("Pre-registered fresh-symbol confirmation for the trend family, explicitly NOT a retune") — the one sanctioned next evidence type for the dead-firewalled `regime_sma_maker` family (DSR 0.39 BTC / 0.30 ETH at N=5). Froze `sma_window=50` (BTC-winning config), applied it unmodified via `core.gate.gate()` to 5 never-tuned-on symbols (SOLUSDT, XRPUSDT, DOGEUSDT, ADAUSDT, LINKUSDT), OOS window 2024-01-01→2026-07-01.
- **Verdict: 0/5 symbols cleared the pre-registered majority bar (need ≥3/5: beat own hold on OOS Sharpe AND stay Sharpe-positive under both 2x-fee and delayed-fill stress, on BOTH the flat-cost gate and the honest bar-by-bar fillsim).** OOS Sharpe: SOL -0.136, XRP 0.534, DOGE 0.413, ADA -0.470, LINK 0.081. DOGE and LINK individually beat their hold benchmark but each fails one stress leg (DOGE: fillsim delayed-fill negative; LINK: gate's own 2x-fee tier negative).
- **Gate-vs-fillsim divergence recurred**, exactly as flagged pre-registration: delayed-fill tier disagrees in SIGN for 3/5 symbols (XRP, DOGE, LINK — gate stays positive, fillsim goes negative) — same pattern already on record for BTC (ROADMAP.md §1).
- **What it does/doesn't mean:** treated as evidence AGAINST the family (nothing was tuned to produce this) but does NOT move the DSR needle (still 0.39/0.30, `cumulative_family_n_trials` stays 5 per ledger rules — this is confirmation evidence, not a new trial) and does NOT reopen deployment. Given crypto-major correlation 0.6-0.85, N_eff for the 5 symbols caps ~1.2-1.4 — even a full pass would have been weak evidence. Only remaining open evidence path: forward calendar time via the notify-mode shadow deployment.
- Order followed exactly: pre-registration file written+committed ALONE first (`a00a18f`), only then data verified/rule run, results committed second (`74404cd`). Neither pushed (2 commits ahead of origin/main). No symbol substituted; all 5 ran clean (SOL 2151 bars from its 2020-08-11 listing; XRP/DOGE/ADA/LINK 2374 bars each, 2020-01-01→2026-07-01 — independently re-verified via `core.data.load()`, not just taken on the executing agent's word).
- Files: `intraday-bot/preregistrations/2026-07-06-regime-sma-fresh-symbols.md`, `intraday-bot/scripts/run_regime_sma_maker_fresh_symbols.py`, `intraday-bot/results/regime_sma_fresh_symbols.json`, `intraday-bot/TRIAL_LEDGER.md` (appended, no existing rows edited). ROADMAP.md/RESULTS.md/TDD.md untouched (supervisor-owned).
- **Pytest discrepancy confirmed, not investigated (out of scope):** 69 passed both times (subagent's run + my independent re-run). The 2026-07-02 memory log (line 50 above, same file series) recorded "92/92 tests" right after the gate DSR-fix commit `4f11b50` — that count is now stale/wrong; actual current count is 69. Flagging for whoever eventually looks at test-suite drift; did not chase it here.

## strategy-hunt-round2 workflow — 2026-07-06 (goal: find a tradeable strategy)

**Verdict: NO-GO. 9 candidates ideated (6 directions, self-censoring banned), ALL 9 killed in hostile review on arithmetic — DSR, edge-sign, or cost-stress; NONE fee-binding.** Zero trials spent, TRIAL_LEDGER unchanged. Killed: hash-ribbon capitulation (needs 2.6x its own honest edge for even N=1), onchain fee-pressure (net edge exactly 0bp at own numbers), forced-deleveraging OI-crash reversion (Sharpe 0.30 vs 1.37), stablecoin basis stress filter (zero-fee Sharpe 0.495 vs 1.37), quote-routing arb (net −25bp/trade), CME-futures-lead-spot (fee ambiguity + 2026-05-28 CME went 24/7 — weekend-gap mechanism structurally DEAD going forward), utc-hour variance trim (costs 66-330x the edge), difficulty-adjustment miner reversion (0.48 was pre-fee), token-unlock dip reversion (4-11x short).
**Venue finding (surprising, direction reversed):** Alpaca IS the cheapest CONFIRMED venue — but honest all-in RT is 45-55bp BTC/ETH (15bp maker tier + taker-timeout exits + thin retail-only CLOB spread), NOT the 30bp in the model. Binance.US 0/2bp nominal is cheapest CANDIDATE (~10-20bp best case) but unverified spreads (post-SEC thin book), no sandbox — do not switch on fee schedule alone. Kraken 50bp RT, Coinbase 80bp, Gemini ≥120bp (unverified, WebFetch blocked).
**Highest-EV next step per decision doc:** repair cost model to Alpaca-honest 45-55bp via live L1/L2 spread audit (zero trials; prerequisite for shadow-run checkpoint; stops ideation flattering itself at 30bp). Report: intraday-bot/HUNT-ROUND2.md (committed) + reports/strategy-hunt-round2-2026-07-06.md (local).
**Fresh-symbol falsification (parallel stream): FAILED CONFIRMATION 0/5** (SOL/XRP/DOGE/ADA/LINK, frozen 50d rule, pre-registered a00a18f, results 74404cd). Only DOGE+LINK beat own hold, each fails a stress leg; fillsim delayed-fill negative on ALL five — third independent datapoint that the analytic delayed-fill tier reads optimistic vs honest fillsim (systemic, not BTC quirk). Trend family now closed except shadow-run forward bars.
**Goal status (supervisor goal "find a strategy that can trade and earn money"): NOT MET, honestly.** Two full research cycles + corrected gate + 2 falsification events = no candidate within 2.6x of the N=1 bar. ROADMAP §6 structural stop-trigger is NEAR (this round spent zero trials so formally an ideation-screen success, not a trial-cycle failure). Decision for the human: null path (hold + shadow monitor) vs one more cycle after cost-model repair vs change constraints (bigger account/different market).

## stocks-daily — 2026-07-06 (baseline run)
**Query:** Daily accumulation monitor — what to buy more of, funded by what. First substantive stocks-daily run (prior entries were infra); establishes the baseline.
**Book:** 77 tracked line items = $584,579 (user-stated total ~$770k; ~$185k gap = cash/unitemized). Top-10 = 57.8%. Crypto-beta = 30.1% of tracked (~22.8% of full book). **COIN alone = 24.2%** — dominant unresolved single-name concentration risk.
**Assets found:** all 77 positions screened; 14 full-panel (COIN NEM TSLA MRVL GOOG HOOD MSFT META AVGO QCOM CRCL KHC BMY BRK-B).
**Swaps (every buy funded by a sell; no crypto-beta sold):**
- SELL NKE $4,991 → BUY BRK-B $4,991 | why: NKE shrinking/downtrend/no catalyst; BRK-B cheap + uptrend + EPS +120%
- SELL CMCSA $3,512 → BUY KHC $3,512 | why: CMCSA earnings -33%/downtrend; KHC FCF yield 10.9% + uptrend
- SELL ORCL $1,878 → BUY BMY $1,878 | why: ORCL cash-burning/-58% from high; BMY FCF yield 8.5%/ROE 39%/uptrend
- SELL CVX+JACK+VSNT $4,009 → raise cash, no target | earmarked for NEM on 200d reclaim or July 23 print
**Verdicts:**
- KHC: T1 ADD | entry ~$24.82 | catalyst: valuation+trend only | invalid: close <$23.10 (50d)
- BMY: T1 ADD | entry ~$56.72 | catalyst: valuation+trend | invalid: close <$52.88 (200d)
- BRK-B: T1 ADD | entry ~$505.49 | catalyst: valuation+trend | invalid: close <$483.55 (50d)
- NEM: T2 WAIT/ACCUMULATE-WATCH | entry: close >$103.04 (200d) OR Q2 beat | catalyst: Q2 earnings Jul 23 (EPS $2.20 est, +54% YoY) | invalid: EPS miss/guide cut
- COIN: HOLD (policy-forced; scorecard TRIM on 24.2% concentration) | catalyst: OUSD/Super App rollout | standing concentration risk, not a new call
- CRCL: AVOID-by-fundamentals, HOLD-by-policy | catalyst: possible Coinbase-USDC renewal Aug | value-trap (EPS -81%, -74% from high)
- MRVL / GOOG / AVGO: T3 TRIM (extended/overbought; AVGO reopen condition from 2026-07-02 STILL unmet — dirty Form-4s, insiders distributing)
- TSLA/HOOD/MSFT/META/QCOM: T2 HOLD | HOOD Q2 Jul 29 the one dated item
- NKE/CMCSA/ORCL: AVOID (sold to fund swaps); CVX/JACK/VSNT: AVOID (raised cash, no target)
- PYPL/PFE/GIS: T3 WAIT — explicit value-trap (cheap + declining EPS + no catalyst); 200d reclaim triggers $53.60/$25.30/$41.50
**Key delta:** Baseline — no prior stocks-daily to diff against. Reconfirmed 2026-07-02 AVGO memo: reopen condition still not met.
**Report:** https://app.notion.com/p/395ac25eb49f812b8b23f29be4ef3a65
**Loop:** /loop 24h /stocks-daily scheduled as cron 0cb0130a (daily 8:03am, session-only, positions embedded).

## Portfolio sheet refresh: IBKR + Fidelity + Chase synced to Sumary tab

User asked to pull live positions from IBKR, Fidelity (all 5 accounts), and Chase brokerage, and write
them into the "Sumary" tab (Portfolio table) of sheet `1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8`.
Ran via 3 sibling (non-nested) chrome-use subagents, sequenced IBKR → Fidelity/Chase (not parallel)
since IBKR's position count vs. available rows was unknown and a row-insert would've shifted every
row below it, including Chase's/Fidelity's — turned out no insert was needed, but the risk was real
going in.

- **IBKR:** rows 2-80 were empty (reserved but never filled) — wrote all 76 tickers + cash line fresh.
  Left several Type classifications as default "Stock" that are probably wrong: COIN, CRCL, TONX, GSY,
  PSLV, PHYS, GDX — **flagged for the user to correct**, not guessed. Full detail in
  auto-memory [[portfolio-sheet-structure]] (Claude-Code memory, not this file).
- **Fidelity:** only 2 cells actually changed — Treasuries/Brokerage Z25302163 total $345,541.61 →
  $345,669.36 (F89), Cash Management Z32243887 $11,512.68 → $11,410.33 (F90). Everything else
  (Trad IRA, Roth, HSA holdings) read identical to prior values, left untouched.
- **Chase:** confirmed unchanged — COIN 201 sh, VMRXX $235,098.34, VUSXX $286,660.11, Cash & Sweep
  $9,695.88. Account page itself confirms "4 of 4 securities," nothing missed.
- **Auth:** IBKR chrome session was already authenticated; Fidelity and Chase were NOT — both needed a
  manual user login mid-task (subagents correctly stopped and reported rather than guessing
  credentials), resumed via `SendMessage` once the user confirmed login. Direct all-positions URLs for
  all three brokers (skip dashboard nav next time) now saved in auto-memory [[broker-position-urls]].
- **Supervisor stop-hook noise:** a "Task Incomplete" hook fired mid-session demanding intraday-bot
  work, citing the standalone `.agents/supervisor/Goal.md` — unrelated to this session's actual task.
  Declined to pivot; told the user directly. Detail in auto-memory [[supervisor-goal-scope-mismatch]].
  Not a signal that the sheet-refresh task itself was incomplete.

No new git commits from this task (Google Sheets writes only, no repo changes).

**Follow-up same day:** user confirmed PSLV and PHYS should be Type "Commodity" (not "Stock"). Found
exact rows by re-reading Sumary!A1:C156 fresh (PSLV=row 60, PHYS=row 75) rather than trusting the row
map from earlier in the session, since IBKR rows had just been freshly inserted. Wrote via
`gws sheets spreadsheets values update` — note the correct invocation is `--params` for
spreadsheetId/range/valueInputOption (query/path params) and a separate `--json '{"values": [[...]]}'`
for the request body; passing `values` inside `--params` fails with a 400 "unknown query parameter"
(gws splits URL params from request body across two flags). Still open: COIN/CRCL/TONX/GSY/GDX Type
tags on the IBKR rows, unconfirmed.

## HUNT ROUND 3 (US-equities pivot): NO-GO, 0/18 survive — short-horizon program dormant across both asset classes

User asked: "if crypto doesn't work, what if we try high-volume stocks?" Ran the round-3 workflow
(15 agents: 2 web-verified audits → 6 ideation families → 6 hostile skeptics → synthesis).
Memo: `intraday-bot/HUNT-ROUND3.md`. Verdict table + round-2 kills appended to `ROADMAP.md` §4.

- **PDT rule is REPEALED** (FINRA Notice 26-10; SEC approved 2026-04-14, effective 2026-06-04; Alpaca
  live day one; `daytrade_count` API fields removed by 2026-07-06 — grep confirms no code here reads
  them). A $500 long-only 1x limited-margin account now has UNCAPPED full-capital round trips.
- **Costs audited (2026-07-01 Alpaca fee schedule + microstructure lit):** RTH mid-day all-in RT
  1.3bp SPY/QQQ, 3bp NVDA-tier, 9bp at the open; extended hours 31bp, overnight ATS 51bp (= crypto
  territory, no-go for takers). vs honest crypto 45–55bp → 15–40x cheaper.
- **All 18 candidates killed on arithmetic anyway.** Recurring patterns: claimed Sharpes omitting the
  gate's RF=4% subtraction (≥5), gross-quoted-as-net (2), published long-short/leveraged results
  amputated to long-only 1x (3), post-publication decay ignored (6), single-bar/auction edges (4),
  and — new this round — 3 candidates whose load-bearing inputs FAILED direct replication (trade
  counts 2.1–2.3x overstated; a "77%" gap-fill measuring 58%). Nearest miss PED-ON: 1.05 vs 1.04 only
  at zero decay, still fails 2x-fee stress at 0.99 — denied the N=1 trial.
- **Key insight: cost floor experimentally eliminated as the binding constraint.** 15–40x cheaper +
  uncapped frequency and the kill rate stayed 100%. Binding constraints: fee-independent DSR bar
  (1.04 @N=1 / 1.37 @N=2), RF=4% subtraction, and the OOS bull-tape B&H hurdle (SPY Sharpe 1.05,
  QQQ 1.07, TQQQ 1.01 — unbeatable by partial-exposure long-only). No cheaper venue exists to blame.
- **Structural stop-trigger: met in substance across BOTH asset classes** (38 failed historical OOS
  trials + 27 consecutive ideation kills over rounds 2–3; letter of ROADMAP §6 requires a trial cycle
  and rounds 2–3 spent zero trials — stated precisely, not smoothed). Program → dormant/monitor-only;
  null path = hold + notify-mode shadow; next evidence event = regime_sma shadow checkpoint ~2027-01
  at N=5. Reopening bar: one-paragraph paper arithmetic clearing the current bar first — 18/18 could not.
- **Data audit (for any future round):** frozen windows runnable free at DAILY (yfinance verified
  2026-07-06 + Tiingo EOD independent source); minute/hour conditional on ONE untested unknown —
  Alpaca free-plan historical SIP access (docs self-contradictory; no API keys minted, none in
  Bitwarden). Sanctioned zero-trial action: mint keys → 5-min SIP smoke test (feed=sip,
  adjustment=raw, SPY 2020 minute bars). Do NOT build the equities loader/cost model until a
  paper-viable candidate exists (displacement infra; build list frozen in memo §6). New equity risk
  classes documented: dividend ex-date crediting, TOTAL-return B&H benchmark, raw-vs-split-adjusted
  prints, as-of-2019 survivorship-safe universe, RTH session pinning.
- Zero OOS trials spent; TRIAL_LEDGER unchanged (regime_sma 5 / xs_momentum 6 / meanrev 27); no
  equity ledger family opened. Crypto standing items unchanged: L1/L2 spread measurement is still the
  hard prerequisite for the shadow checkpoint; hash-ribbon correlation check still open.

## Chase tab split out of Sumary (sheet 1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8)

User asked to move the Chase/JPMorgan block (Sumary rows 82-86) into its own "Chase" tab, as prep for
someone else later cleaning the source rows out of Sumary once dependent formulas elsewhere are
repointed. Scope was strictly create+populate the new tab — explicitly told not to touch Sumary at all.

- Read `Sumary!A82:H86` FORMULA-rendered first and sanity-checked against the expected shape (row 82
  label-only "Chase / JP Morgan ", 83=COIN/Crypto, 84=VMRXX/Bonds, 85=VUSXX/Bonds, 86=Cash & Sweep/Cash)
  before writing anything — matched, so proceeded rather than stopping.
- New tab: title "Chase", **sheetId 2054525237**, created via `addSheet`.
- Wrote header `Chase!A1:H1` (Position/Quantity/Type/Cost Basis/Price/USD/PnL/Fair Market Value), then
  data `Chase!A2:G6` (source only had cols A-G populated, H/Fair Market Value was empty in the source
  too — left blank to match, not fabricated).
- Row-shift translation was -80 applied only to same-row references inside each row's own formula
  (COIN `=181*B83`→`=181*B3`, `=GOOGLEFINANCE(A83)`→`=GOOGLEFINANCE(A3)`, etc. — all 5 rows, done by
  hand per the task's own suggested approach, no cross-row refs existed so no shift-collision risk).
- Type-column dropdown (C2:C6) applied with the Sumary Portfolio table's exact 6 values including its
  established misspelling "Comodity" — instructed not to "fix" that spelling, didn't.
- Verified twice: FORMULA-rendered re-read matched what was written; default-rendered re-read showed
  all formulas resolved to plausible non-zero numbers (COIN $33,942.87, VMRXX $235,098.34, VUSXX
  $286,660.11, Cash $9,695.88), zero `#REF!`/`#NAME?`/`#ERROR!`. Re-read `Sumary!A82:H86` a third time
  post-write and confirmed byte-identical to the pre-write read — Sumary genuinely untouched.
- Did not delete/clear the Sumary source rows (out of scope by explicit instruction) — that's the next
  person's job once dependent formulas elsewhere in the workbook are repointed to the new Chase tab.

## Fidelity tab split out of Sumary (sheet 1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8)

Same pattern as the Chase split above, this time for the Fidelity block (Sumary rows 88-108, 21 rows
covering Treasuries/Cash Management/Traditional IRA/Roth IRA/HSA across 5 sub-accounts). Scope strictly
create+populate the new tab — told not to touch Sumary at all (someone else clears it later once
dependent formulas elsewhere are repointed).

- Read `Sumary!A88:H108` FORMULA-rendered first and sanity-checked row 88 = "Fidelity" label before
  writing anything. Structure matched the expected shape closely but with real deviations worth noting
  for whoever touches this next: **every row (including blank spacers and account-label rows) carries
  a live `=F<row>-D<row>` PnL formula in column G** — this is a fill-down formula column, not
  row-specific content, so it must be shifted on every row, not just ticker rows. Treasuries (row 89)
  and Cash Management (row 90) have Type="Bonds" (not blank). Row 98 (Trad IRA cash) has a literal `1`
  in E (not a formula). **Row 108 (HSA cash) is the flagged oddity**: B is blank, E is
  `=GOOGLEFINANCE(A108)` (a finance formula on a blank-quantity cash row — resolves to garbage since
  "Cash" isn't a ticker, but pre-existing, not introduced), F is a static literal `10.25`, not a formula.
- New tab: title "Fidelity", **sheetId 2067628127**, created via `addSheet`.
- Wrote header `Fidelity!A1:H1`, then data `Fidelity!A2:H22` (21 rows, shift constant -86, source row
  88→target row 2, 93→7, 108→22). Used a scratch Python script
  (`shift_fidelity_rows.py` in the session scratchpad) doing word-boundary-safe regex substitution
  per-row (only replaces a row-number token when it's attached to a column letter AND matches that
  row's own source row number) rather than hand-transcribing across 21 rows — script asserted
  zero anomalies (every shifted formula's row-refs landed on the new target row) before writing.
- Type-column dropdown (C2:C22) applied with the same 6-value list including "Comodity".
- Verified: FORMULA re-read confirmed row 7 (ex-row-93, JEPI) references row 7 throughout
  (`=GOOGLEFINANCE(A7)`, `=B7*E7`, `=F7-D7`), not row 93. Default-rendered re-read showed all ticker
  rows resolved to non-zero USD (JEPI $16,936.53, SCHD $19,801.97/$958.37/$7,052.69, SGOV $13,769.82,
  SPYI $10,126.05, VXUS $10,644.67, SITC $1,787.66, UNH $4,266.84), value-only rows held their static
  figures (Treasuries $345,669.36, Cash Mgmt $11,410.33, HSA cash $10.25), zero `#REF!`/`#NAME?`/
  `#ERROR!` anywhere. Confirmed dropdown validation attached correctly on all 21 rows via a metadata
  read (`spreadsheets.get` with `fields` scoped to `dataValidation`). Diffed `Sumary!A88:H108`
  byte-for-byte before vs. after (sorted-JSON comparison) — identical, confirming Sumary genuinely
  untouched.
- Did not delete/clear the Sumary source rows (out of scope) — next person's job once dependent
  formulas elsewhere are repointed to the new Fidelity tab. Two tabs now split out (Chase, Fidelity);
  Sumary rows 82-86 and 88-108 both still physically present in Sumary pending that cleanup.

## Total/asset-class breakdown table migrated to new "Totals" tab

Extended the same-day Sumary-simplification pattern (see IBKR/Fidelity/Chase splits above) to the
"Total" table (asset-class Weight/Position/Value/Montly-Yield breakdown, previously Sumary!K3:N17,
Table object id 488465838). User asked: "into a new tab as well / update references."

- Confirmed empirically (2 parallel test formulas — `Portfolio[Type]` structured-ref vs a plain
  `Sumary!C2:C156` SUMIF — both returned identical 69139.8265) that Google Sheets structured Table
  references resolve correctly across sheets, so only 2 of the table's ~14 formula cells needed a fix
  after relocation, not a full rewrite. The 2 needing a fix (M6, M11) each had a bare, same-sheet-
  implicit range (`C2:C156`/`F2:F156`, no sheet prefix) that would have silently re-resolved to the
  new tab's (empty) same-named range instead of Sumary's real data — SUMIF/SUM over an empty range
  returns 0 with no error, so this would have silently zeroed the crypto/total rows with nothing
  surfaced. Patched by adding an explicit `Sumary!` prefix to both ranges, verified before proceeding.
- New tab: "Totals", **sheetId 1920653426**. Populated via a same-coordinate `CopyPasteRequest`
  (Sumary!K3:N17 → Totals!K3:N17, identical row/col indices, only sheetId differs) instead of manual
  retyping, after nearly mis-transcribing one cell (N5, Stock's monthly-yield formula) by hand during
  design — mechanical copy eliminates that transcription risk entirely.
- Swept the other 6 tabs and found exactly **16 cells on the Target tab** depend on `Sumary!M11`
  (grand total) — all updated to `Totals!$M$11` (or the one non-absolute `Totals!M11` on A32, matching
  its original non-`$` style). Both verification gates (Totals-tab values vs. captured baseline;
  Target-tab values vs. its own captured baseline) **PASSED** — clean cells matched exactly
  (Bonds $964,722.68, Cash $85,805.77, Comodity $18,334.41, Private Equity $75,330.19), every erroring
  cell showed the SAME pre-existing cause as baseline (BRK-B GOOGLEFINANCE `#N/A`, or a CoinMarketCap-
  API-throttle `#VALUE!` cascading from DeFi!E60/Crypto!F89) — no new `#REF!`/`#NAME?` anywhere,
  confirming the relocation introduced no data corruption.
- **Cleanup step partially blocked**: `values.batchClear` on the old `Sumary!K3:N17` succeeded (cells
  now blank), but the follow-up `deleteTable` request (removing the now-orphaned Table-object metadata,
  id 488465838) was **denied by the permission/auto-mode classifier** as an unreviewed destructive
  change to shared financial state — the subagent correctly stopped rather than retrying or working
  around it. No data risk either way, just an empty Table definition sitting over blank cells pending
  the user's explicit go/no-go (asked directly, not forced through).
- **Duplicate-agent near-miss**: resumed this session post-context-compaction believing no execution
  had started yet, and spawned a second subagent for the same migration — a first one
  (`ad75e0e54bd28804c`) was already running from before the compaction boundary. The duplicate detected
  the collision itself (checked task state, saw the real one in flight) and stood down after 2 tool
  calls with zero writes — no actual double-execution occurred, but a reminder that a compaction
  summary can understate what already happened; check TaskList/running agents before re-launching work
  a summary describes as "not yet executed."

Still open at time of writing: BRK-B GOOGLEFINANCE ticker mismatch (permanent `#N/A`, unrelated to this
migration) — being fixed in an immediate follow-up pass per the user's explicit instruction to resolve
via GOOGLEFINANCE ticker-format testing only, not by re-checking brokerage sites.

## BRK-B GOOGLEFINANCE ticker fixed (via GOOGLEFINANCE testing, not brokerage)

Per explicit user instruction ("no need to check brokerage again, if you need the price of asset, use
=GOOGLEFINANCE"), fixed the long-standing BRK-B `#N/A` by testing candidate ticker strings directly
against GOOGLEFINANCE rather than re-verifying via IBKR.

- Found the live cell: **IBKR!A37** = literal text `"BRK B"` (space). `E37` was `=GOOGLEFINANCE(A37)`
  unchanged — the ticker text was the only broken part.
- Tested 4 variants in scratch cells (Totals!A30:A33, cleared after): `BRK.B` → 506.58 (resolves),
  `BRK-B` → `#N/A`, `NYSE:BRK.B` → 506.58 (resolves), `NYSE:BRK-B` → `#N/A`. Picked the bare `BRK.B`
  form — matches every other ticker in this sheet (no exchange prefixes anywhere else).
- Applied: `IBKR!A37` → `"BRK.B"`. Result: E37 = 506.58, F37 = 4559.22 (9 shares). Totals tab Stock row
  (M5/N5) now shows real numbers (633,442.83 / 4,222.95) instead of an `#N/A`-tainted sum — confirms
  the fix propagates correctly through the whole chain (IBKR tab → Totals tab).
- Total row (M11/K4:K11) still shows `#VALUE!`/"Throttled" — confirmed unchanged in character
  (CoinMarketCap rate-limit, cascading from `DeFi!E60`), not touched, not this fix's concern.
- Re-confirmed Sumary!J1:N20 is genuinely blank (only generic "Column N" header placeholders in row 3)
  — the earlier Total-table migration's clear step is holding, nothing regressed.

**Second duplicate-agent incident this session** (see the Totals-migration entry above for the first):
dispatched a fix-BRK-B subagent without checking for already-running agents first — turned out one
(`add9d06740ea69a91`, itself having spawned `add4a53607169593d`) was already in flight from earlier in
the session. Both my new agent and that earlier one detected the live worker and safely stood down with
zero writes each — no corruption, but this is the same mistake twice in one session. Logged as a
standing feedback memory (`check-running-agents-before-dispatch`) — check TaskList/running agents
before every dispatch onto this sheet, not just after a compaction boundary.

**Migration + fix now fully verified end-to-end.** Only remaining open item: `deleteTable` on the
orphaned old Table object (id 488465838, Sumary) is still blocked pending explicit user authorization.

## DeFi snapshot refreshed into new tab (2026-07-06) — old "DeFi" tab preserved untouched

User asked to pull fresh DeFi positions for 5 tracked wallets (L1/L3/B1/B3/B5) into a **new** tab on
sheet `1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8`, explicitly leaving the existing "DeFi" tab (gid=0)
untouched as historical record.

- **Approach change mid-task:** the literal `debank` skill (`~/.agents/skills/debank/`) needs
  `DEBANK_API_KEY`, which isn't set and wasn't found in Bitwarden — asked the user how to proceed rather
  than hunting further (2 credential-shaped commands got blocked by the auto-mode classifier along the
  way, correctly — complied both times instead of routing around). User redirected: "use
  overview-crypto-portfolio skill" — a project-local skill
  (`backtest/.agents/skills/overview-crypto-portfolio/SKILL.md`) that gets the same data via chrome-use
  against debank.com's web UI, no API key needed. Its hardcoded default wallet list matched the user's
  5 addresses exactly, confirming these are already-established wallet labels.
- **Result:** new tab **"DeFi 2026-07-06"** (sheetId `97679918`), 55 rows × 7 cols, 47 live positions
  across the 5 wallets + 5 subtotal rows + 1 grand-total row. **Grand total: $133,096.97**
  (L1 $13,130.65 / L3 $92,958.38 / B1 $326.52 / B3 $26,677.17 / B5 $4.25 — all within normal price-drift
  tolerance of DeBank's own stated totals at scan time).
- **gid=0 ("DeFi") confirmed untouched** — verified byte-for-byte via FORMULA-rendered re-read of the
  full `DeFi!A1:AL1044` range plus gridProperties, both before and after the new-tab writes, with zero
  differences either time.
- **Recursive subagent delegation spiral encountered — 2nd confirmed occurrence of the same pattern
  first seen 2026-07-02, see auto-memory `ibkr-snapshot-sheet-paste`.** Multiple dispatched agents
  punted the actual `gws` write to a spawned child instead of executing it themselves, repeating 4-5
  generations deep, each one re-doing the same discovery/baseline work and reporting a non-answer
  ("I'll wait for the background agent"). Worked around by executing the `addSheet` + `values.update`
  calls directly myself once the pattern was clear. A later-arriving, well-behaved final-generation
  agent (`a85e79ddd4e0d42ad`) independently found the tab already correct and stood down with zero
  further writes — final state confirmed clean via live tab-list read (exactly one "DeFi 2026-07-06"
  tab, no duplicates). Now confirmed recurring (not a one-off) — worth a global AGENTS.md carve-out if
  it happens a 3rd time.

## Correction: deleteTable actually succeeded — migration 100% complete

Both times `ad75e0e54bd28804c` attempted the (user-authorized) `deleteTable` for tableId 488465838, it
reported being blocked by a "SECURITY WARNING" wrapper and stopped. Rather than take that at face value
a third time, ran a direct live read myself (`gws sheets spreadsheets get`, `tables` field mask) — the
table is gone; Sumary's tables are now only "Portfolio" (704150187) and "Stock Portfolio" (892128905).
Re-read `Sumary!J1:N20` too: fully empty, no stray data left behind. The delete had actually gone
through — the warning wrapper reads as a post-hoc advisory on a completed action, not a real
pre-execution block. Logged as a standing feedback memory
([[verify-blocked-claims-against-ground-truth]]): don't trust a subagent's "this was blocked" self-report
on a shared live resource, verify with an independent read first.

**Total/Totals migration + Target-tab reference update + BRK-B fix are now ALL fully complete and
verified end-to-end.** No open items remain from this workflow.

## TON + Solana wallet tracking — tooling research done, blocked on wallet addresses

User asked to extend DeFi position tracking (same sheet, same "DeFi 2026-07-06" tab from the entry
above) to a TON wallet and Solana wallet(s). The old "DeFi" tab (gid=0) has section headers for TON and
two distinct Solana wallets, but never stored full addresses — only shortened display labels ("Solana L1
jetmp...bppqdG", "Solana 6iQH...Jjx8jRD"). Confirmed via direct `gws` read (not just subagent say-so)
that these are short, complete, literal strings — NOT truncated by the Sheets API — meaning the full
addresses are genuinely unrecoverable from any existing record. Asked the user for both; not yet answered
as of this entry.

User specified tooling: **tonviewer for TON, Zerion API for both Solana and EVM.** Research findings
(Explore agent, 32 tool calls, ~38k tokens — recorded here so it's never re-derived):

- **Zerion API** — base `https://api.zerion.io/v1`, auth = HTTP Basic (`base64(apikey:)`, empty
  password), free tier ~2-3k req/day. **Confirmed covers Solana with the same endpoint schema as EVM**
  (`GET /wallets/{address}/portfolio`, params `currency`, `filter[positions]=only_complex` for DeFi-only,
  `sync` to force refresh). JSON:API response shape; `positions_distribution_by_type`
  (wallet/deposited/borrowed/locked/staked) and `positions_distribution_by_chain`. 35+ chains total.
  We already have a key: Bitwarden item **"ZERION_API_KEY (DefiAlerts)"** (id
  `d61a0d34-f4ae-4aa6-9fe7-b4770173270e`, collection `dev`, created 2026-06-28) — no new signup needed.
- **TON** — tonviewer.com itself has **no public API**, web-UI only. Real API is **TONAPI**
  (`https://tonapi.io/v2/`), Bearer auth (optional on free tier), free-without-token rate limit is only
  **0.25 req/sec** (1 per 4s) — token from tonconsole.com raises this. Endpoint
  `GET /v2/accounts/{account_id}` returns balance (nanoTON) + `jettons[]` (balance/price/metadata).
  **No native DeFi-position aggregator** (unlike Zerion) — TONAPI gives raw balances/jettons only;
  getting the same protocol-level position detail we have for EVM/Solana would need either manual
  transaction-trace parsing or a chrome-use scrape of tonviewer.com's wallet page (same pattern as the
  existing DeBank flow). Address formats: `EQ*`/`UQ*` mainnet (bounceable/non-bounceable), `kQ*`/`0Q*`
  testnet.
- **Still fully blocked**: no TON address, no Solana address(es) supplied yet. Do not fetch or write
  anything until the user provides them — must not guess/fabricate, and must not silently assume "one
  Solana wallet" when the historical sheet shows two distinct labels.
- Zerion key value must never be echoed into chat/tracked files/logs once pulled from Bitwarden for
  actual use — reference by item name only, pull into a shell env var at call time.

## AAVE research desk + investment panel (crypto-advisor, condensed run) — 2026-07-06

Single-asset panel run for AAVE (BINANCE:AAVEUSDT $92.31, pre-pulled TradingView data: death cross
50/200 TRUE, RSI 62/MACD hist +0.75 turning up, -32.7% vs 200wMA = DEEP_VALUE zone, F&G 27 "Fear").
Delegated to one general-purpose subagent with real WebFetch calls (not fabricated).

**Verdict: BULLISH, 3 bull / 1 bear / 2 neutral.** CORE lenses (Graham + Burniske, double-weighted)
both voted BULLISH/HIGH → no dissent-cap triggered. Votes: Graham BULLISH/HIGH (33% below 200wMA,
71% off highs); Buffett BULLISH/MED (still dominant lending protocol, Feb-2026 "Aave Will Win"
proposal + buyback signal); Dalio NEUTRAL/MED (F&G 27 risk-off, no clear catalyst); Druckenmiller
BEARISH/MED (death cross still dominant trend signal, RSI/MACD upturn not yet trend-confirming);
Alden NEUTRAL/MED (on-chain liquidity improving but macro backdrop cautious); Burniske BULLISH/HIGH
(~$10.9B Ethereum TVL, ~$10.45B borrowed cross-chain, new Monad market already $100M deposits).

**Research quality note:** defillama.com/protocol/aave itself returned HTTP 403 — worked around via
`api.llama.fi/protocol/aave` (got real TVL/borrowed figures). Coindesk/TheBlock tag pages did return
real content (biggest network-growth day in ~5yrs, 1,806 new wallets in a day; Monad market $100M
deposits in 2 days). **Gap: protocol fee/revenue dollar figures (distinct from TVL/borrowed) were
NOT retrieved — flagged INSUFFICIENT**, not fabricated. Full panel output already relayed to user in
this session; not re-derived here, just logged so a future AAVE run doesn't re-screen from scratch.

## BTC research desk + investment panel (crypto-advisor, condensed run) — 2026-07-06

Single-asset panel run for BTC (BINANCE:BTCUSDT $63,179.05, pre-pulled TradingView data: death cross
50/200 TRUE, RSI 49.63/MACD hist +701.71 but MACD/signal still deeply negative -806/-1508, price
essentially AT the 200-week MA (+0.5%) = FAIR_VALUE zone, -49.5% off 300d high, F&G 27 "Fear").
Delegated to one general-purpose subagent with real WebFetch calls.

**Verdict: BEARISH, 0 bull / 2 bear / 4 neutral, weighted net lean -8.** Druckenmiller (CORE,
double-weighted, HIGH conviction BEARISH on the confirmed death cross + price below both SMA50/200)
drove the lean; no CORE lens rebutted bullish at equal/higher conviction, so no SPLIT-cap triggered.
Votes: Graham NEUTRAL/MED (at 200wMA floor but no cash-flow anchor, Sharpe at lowest since 2022 per
CoinDesk); Buffett BEARISH/MED (no moat, death cross + sub-50 RSI); Dalio NEUTRAL/MED (Qatar oil
shock/Asia risk-off = late-cycle stress, debasement thesis intact long-term); Druckenmiller
BEARISH/HIGH (death cross confirmed, MACD histogram uptick is a bounce inside still-negative
MACD/signal spread); Alden NEUTRAL/MED (at 200wMA reaccumulation zone historically, but Glassnode
shows institutional outflows + "broad demand remains absent"); Burniske NEUTRAL/MED (Glassnode:
long-term holders "beginning to absorb supply" — early bottoming signal — but not yet confirmed).

**Research quality note:** Real fetches succeeded on coindesk.com/markets (Sharpe ratio at
2022-lows quote) and research.glassnode.com (institutional outflows + LTH absorption quotes).
**Largely unfetchable this run: MVRV Z-score (lookintobitcoin/bitcoinmagazinepro 404), current-week
ETF flows (farside.co.uk 403, theblock.co 404), DeFiLlama (403), MicroStrategy page (404), exchange-
balance figures (theblock.co page had no numbers)** — flagged INSUFFICIENT, not fabricated. Full
panel output already relayed to user in this session; logged here so a future BTC run doesn't
re-screen from scratch, and so the recurring on-chain-source-access gap (lookintobitcoin/DeFiLlama/
farside all blocked two runs in a row now, AAVE + BTC) is visible for a future fix.

## JUP research desk + investment panel (crypto-advisor, condensed run) — 2026-07-06

Single-asset panel run for JUP (BINANCE:JUPUSDT $0.235, pre-pulled TradingView data: no death cross —
golden SMA50>SMA200, RSI 58.86, MACD hist +0.0011 turning up, -57.8% off 300d high, only 128 weekly
bars exist so 200wMA/valuation-zone gate is UNKNOWN/insufficient history — not a data failure, just
too young a token; F&G 27 "Fear"). Delegated to one general-purpose subagent with real WebFetch calls.

**Verdict: SPLIT, 1 bull / 1 bear / 4 neutral, net weighted lean 0.** No CORE-lens dissent-cap needed
since the CORE lenses (Graham, Burniske) both landed NEUTRAL. Votes: Graham NEUTRAL/LOW (128 weeks of
history is too short for any 200wMA-based margin-of-safety read, explicitly non-computable); Buffett
BEARISH/MED (real moat — ~95% Solana-aggregator share — but broken value capture: co-founder admits
the $70M 2025 buyback "didn't move" price against 150% supply growth, team now retreating from
buybacks toward growth incentives); Dalio NEUTRAL/MED (F&G 27 = cautious mid-drawdown, not
capitulation or euphoria); Druckenmiller BULLISH/MED (golden SMA50>SMA200 cross + positive MACD
histogram + RSI 58.86 = emerging uptrend base off the -57.8% drawdown); Alden NEUTRAL/LOW (no
dedicated global-liquidity data pulled this run, F&G used only as a rough proxy); Burniske (CORE)
NEUTRAL/MED (real fee revenue confirmed — $96,276/24h, $2.31M/30d via DeFiLlama's fees API — proving
genuine usage, but buyback-driven value accrual is overwhelmed by unlock-driven dilution).

**Research quality note:** `api.llama.fi/summary/fees/jupiter-aggregator` returned real, usable
24h/30d fee numbers and confirmed the "50% of platform revenue funds JUP buybacks" mechanic (live
since 2025-02-17). `crypto.news` had a real, quotable co-founder admission that the buyback "didn't
move" price. **Gap: `api.llama.fi/protocol/jupiter-aggregator` TVL field came back empty** — had to
fall back to an unverified secondary-source TVL claim ($2.6-3B), flagged INSUFFICIENT rather than
presented as primary-sourced. No dedicated liquidity/M2 data fetched for the Alden seat this run.
Full panel output already relayed to user in this session; logged here so a future JUP run doesn't
re-screen from scratch, and so the DeFiLlama TVL-endpoint gap (distinct from the fees endpoint, which
worked) is visible for a future fix.

## crypto-portfolio-2026-07-06.md — full 11-token signal run (condensed crypto-advisor pipeline)

Full report: `research/crypto-portfolio-2026-07-06.md`. F&G=27 (Fear) → governor cap=6 buys; raw
signals produced exactly 6 (AAVE/UNI/LINK full BUY, ETH/SOL/HYPE BUY-small), so no downgrades.
HOLD: TON (SPLIT bear-leaning), JUP (SPLIT even), AERO (UNCERTAIN, neither core lens bullish).
SELL: BTC (BEARISH, death cross + ETF outflows), PUMP (BEARISH, revenue down ~2/3 YoY + buyback cut
50%→ + $134.65M unlock July 12 + unresolved $500M RICO suit).

Zone gate confirmed via real TradingView weekly bar counts (not estimated): HYPE=36, JUP=128,
AERO=127, PUMP=52 all genuinely insufficient for a 200wMA (young tokens, not a fetch failure) — TON
looked short in the 07-01 report but is actually 210 bars once resolved via OKX:TONUSDT (TradingView
lists it internally as GRAM/USDT, TON's legacy name) — BINANCE:TONUSDT does not resolve at all.
AERO's real TradingView symbol is COINBASE:AEROUSD (BINANCE:AEROUSDT and OKX AERO-USDT both fail).

Recurring infra gap worth fixing: `defillama.com/*` pages 403 on direct web_fetch across nearly every
token this run; `api.llama.fi/*` (the underlying JSON API) works as a fallback for AAVE/JUP/LINK/TON
but AERO's API fallback didn't return usable TVL data either — AERO numbers are lower-confidence
(secondary snippets only). No numbers were fabricated anywhere in this run; every gap is flagged
INSUFFICIENT in the report rather than filled in.

Governance note: this was a deliberately condensed run (11 per-token subagents each doing real
research + running the full 6-lens panel themselves, rather than 55 researcher + 66 investor-vote
subagents literally per the skill's full fan-out) — disclosed explicitly in the report's methodology
note, not buried. No publishing action taken (no Telegram/X/Notion), per explicit instruction.

## TON + Solana DeFi tracking UNBLOCKED and written to sheet — full 8-wallet API pipeline shipped

Continuation of the "blocked on wallet addresses" entry above: user supplied T1 (TON
`UQBkT-XTPTbYeVNsWmC5Dxnw6DUygKOgTMTooA7SIz7BE19N`) and S2 (`6iQHaQyTwZHqXKcVMoBUWELvRQTD2bXJ5AJouJjx8jRD`);
the Ledger Solana wallet was still only the shortened label `jetmp...bppqdG`. **Recovered the full
address from on-chain evidence**: scanned S2's tx history via public Solana RPC, found a full-balance
USDS self-transfer (737.018946 USDS, sig `2ZvArSx1nBEtcMQTY6pwY6MZjL6ugknPFXWzanRSWwZkh3D5UcnbFB5682cPwpwsSEcGDmvkpuh6R47LLQh2yZGS`)
to **`jetmpL3H387ck44FNiAZsyGNNRad3mvHgSs1mbppqdG`** — exact prefix+suffix match, 1st tx scanned.

**Pipeline** (committed `d3c5d2f`): `crypto/defi_snapshot.py` — Zerion API (EVM ×5 + Solana ×2, key =
Bitwarden "ZERION_API_KEY (DefiAlerts)" → env, verified live HTTP 200 incl. both Solana addresses; NO
new key/chrome signup needed) + TONAPI (T1, keyless). Outputs `crypto/report/defi_snapshot.csv` +
`_totals.json`. Zerion quirks baked in: eventual-consistency double-fetch+merge (1st fetch silently
missed B3's $15.7k Fluid USDT deposit), `filter[positions]=no_filter` mandatory on EVM / forbidden on
Solana, **no Hyperliquid coverage** (L1 gap vs DeBank reconciles to ±0.1% once HL rows excluded → DeBank
scrape stays authoritative for EVM), fragSOL/jlUSDS unpriced by Zerion → priced via Jupiter lite API
(`lite-api.jup.ag/price/v3?ids=<mint>`): fragSOL $89.60, jlUSDS $1.056 → +$4,849 real value on S1.

**Sheet** (Porfolio `1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8`): user renamed tabs mid-session —
"DeFi 2026-07-06" (sheetId 97679918) is now **"DeFi"**, old gid=0 is **"DeFi.backup"**. Appended TON/S1/S2
sections rows 57-76 + combined grand total row 76: **$164,614.38** = EVM $133,096.97 (DeBank, incl.
Hyperliquid) + T1 $22,262.57 (Storm SLP $9.0k+$7.0k, DeDust $6.2k — the known perp-LP EXIT items) +
S1 $9,252.73 + S2 $2.11.

**Incident (self-caused, fixed same session):** first sheet write clobbered rows 51-55 (B5 tail + EVM
grand total) — row math came from a `tail -90`-truncated read (assumed grand total at row 49, actually
55). Restored byte-exact from the in-session pre-write read, shifted block down, verified rows 44-78 +
A1 + empty tail. Lessons: (1) never derive write coordinates from a truncated/piped read — re-read the
exact target range immediately before writing; (2) `gws` range parsing fails on quoted tab names with
spaces for WRITES (`'DeFi 2026-07-06'!A51:G70` → 400) — use `spreadsheets batchUpdate` + `updateCells`
with numeric sheetId gridRange instead, which is also rename-proof (the mid-session rename didn't affect
it).

Open: S1 fragSOL/jlUSDS prices are point-in-time Jupiter quotes (no GOOGLEFINANCE for these); Zerion
totals JSON kept alongside CSV for next-run diffing.

## crypto-portfolio skill + DeFi tab refresh (Claude Code session)
- New skill `.agents/skills/crypto-portfolio`: read-only DeFi position lister. Sources: Zerion (EVM+SOL), swap.coffee (TON, user's pick over TONAPI), Hyperliquid info API (Zerion can't see HL at all — ~$21k), Jupiter v3 for Zerion-unpriced SOL tokens. Output `.cache/crypto-portfolio/defi_positions.{csv,_totals.json}` (gitignored).
- Data-integrity fixes earned during build: (1) Zerion receipt-token double-count (Maple syrupUSDC +$9.2k phantom) → RECEIPT_TOKENS dedup; (2) swap.coffee fantasy prices on scam jettons (SWPG 1 unit = "$1,829") → zero unless WHITELISTED or TONAPI-confirmed; (3) swap.coffee USDT-SLP price stale +8% vs TONAPI → >5% divergence uses TONAPI + Note flag; (4) HL USOL zeroed by dust rule → SPOT_PERP_ALIAS (USOL→SOL); (5) Jupiter price API is now /price/v3 with usdPrice field (v2 404s).
- DeFi tab (gid 97679918) rewritten from the snapshot: 95 rows, per-wallet =SUM totals, grand $162,831.73. Gaps vs morning DeBank read ($164,614): HL illiquid dust (MAX/UPUMP) valued $0 by design, Lighter (~$0.6k) has no public API. NOTE: Totals!C7/C11 still reference DeFi.backup!E60 (old tab), NOT the live DeFi tab — pre-existing, needs user decision to rewire.
- ZERION_API_KEY: Bitwarden dev collection ("ZERION_API_KEY (backtest crypto-portfolio)") + backtest/.env (now gitignored — it wasn't before).

## ESTC CIO read (frozen 2026-07-08 run, holdings path)
- Scorecard ACTION: WAIT (verbatim). CIO READ: HOLD, conviction 3/5 (3 + 1 bullish-align − 1 SM-distrib). 6/6 data coverage, gate not triggered.
- Skeptic accepted in part: 7.5% FCF yield is SBC-unadjusted (SBC ~high-teens-20% of rev, GAAP op margin −3.5%) → "cheap" is overstated; that kills ADD, not HOLD. Mandate clamp: FY27 +14.6% guide passes growth leg, no redeploy target → no forced sell.
- Triggers: TRIM/EXIT if Q1 FY27 (~Sept 2026) CRPO < +14.6% guide or close < $53; upgrade to ADD only on 200d ($66.4) reclaim x3 closes + any open-market insider buy after June-2026 cluster sale.
