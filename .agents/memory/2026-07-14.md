## stocks-advisor tag discovery — 2026-07-14

- `copilot skill list` rejects `.agents/skills/stocks-advisor/SKILL.md` because its parsed frontmatter description is 1051 characters, exceeding Copilot CLI 1.0.70's 1024-character maximum.
- The skill name, YAML structure, compatibility field, and directory layout are valid. The stale `.worktrees/stocks-advisor` checkout does not affect Copilot skill discovery.
- Minimal fix: shorten the line 3 description by at least 27 characters, then confirm `stocks-advisor` appears in `copilot skill list`.

## Skill format validation — 2026-07-14

- Fixed all 18 repository skill-loader failures: 16 descriptions over Copilot CLI's 1024-character limit, one malformed YAML description, and one deprecated skill missing frontmatter.
- Added `.agents/scripts/skills/validate_skill_md.ts` and a versioned `hooks/pre-commit` that validates the complete staged Git-index snapshot of canonical skills, including staged additions/deletions and duplicate names.
- Added temp-git regression coverage for staged-vs-working-tree divergence, unterminated quotes, invalid YAML escapes, trailing quoted content, and valid quoted controls.
- Final results: all 85 canonical repository skills pass local validation and `copilot skill list`; targeted validator tests pass 9/9.
