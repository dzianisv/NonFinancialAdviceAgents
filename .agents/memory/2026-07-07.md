
## /crypto-daily run — 2026-07-06 analysis published
- User invoked /crypto-daily directly. Reused the 1h-old 07-06 crypto-advisor report (condensed run, 11 tokens) rather than re-run — market moved <1h; copied to research/crypto-portfolio-2026-07-07.md (feeds loop d6a4ed2d).
- Signals: F&G 27 Fear, governor cap 6. BUY: AAVE $92.31, UNI $3.138, LINK $7.879. BUY(small): ETH, SOL, HYPE. HOLD: TON, JUP, AERO. SELL: BTC $63,179, PUMP.
- Publish: Notion ✅ (page 396ac25e...), Telegram ✅ @CryptoAiInvestor msg_id=36. X.com ❌ — Chrome not running (tweet prepped 194 chars, NOT faked). To finish tweet: launch Chrome logged into x.com, re-run Step 4.
- Auto-eval 3.0/5 (LOW). Gap: condensed run skipped the Verdict Critic pass for all tokens AND omitted a concrete https citation list — neither disclosed in report methodology note. Fix before next publish: reinstate Critic step or disclose its omission. Row in .cache/crypto-advisor/crypto-advisor.eval.csv (commit a50a947).

## Portfolio Google Sheet — broker split already done; move left broken formulas (2026-07-07)
Goal "create a tab per brokerage, move IBKR/Fidelity/Chase to separate tabs, simplify" was ALREADY COMPLETED by a prior session — verified live via gws Sheets API (sheet `1aunLbpNGo85WqrMHiIsy6nFUija4Lnjot-rIhE-pGU8`):
- **IBKR / Fidelity / Chase tabs exist, fully populated with LIVE formulas** (`=GOOGLEFINANCE(A)`, `=E*B`, `=F-D`) — NOT static. Clean, zero errors: IBKR ~$590k, Fidelity ~$445k, Chase ~$565k.
- **Sumary** already trimmed to residual brokers only (Schwab/Vanguard/Treasurydirect/Wealthfront/robinhood/webull/cash banks ~$194k, rows 2-44) + a cross-tab breakdown panel (rows 61+) that already SUMIFs across `$A$1:$A$44` + IBKR/Fidelity/Chase tabs.
- **Totals tab** (index 0) is the canonical rollup: `C11` grand total = `SUM(Sumary!F2:F44)+DeFi!E60+SUM(IBKR!F:F)+SUM(Fidelity!F:F)+SUM(Chase!F:F)`; weights = `=TRUNC(Cn/$C$11,3)`.

**Bug the move introduced (FIXED):** Sumary weight panel E62:E71 were `=B62/#REF!` (denominator deleted in the split). Backed up tab as "Sumary BACKUP 2026-07-06" (id 106637148), then repointed all 10 → `=B62/Totals!$C$11`.

**Pre-existing bug (NOT from the move, still OPEN — user chose "fix seats first" for stocks, DeFi resilience question still unanswered):** `Totals!C11` shows `#VALUE!` because `DeFi!E60 = SUMIF(I2:I55,"<>reinvest",E2:E55)` and `Crypto!F89 = SUM(Table1[Current value])` both error on 4-5 flaky GOOGLEFINANCE crypto price cells (avalanche/POL/PAXG/OP + Crypto row 39). One bad crypto price nukes the whole grand total + every weight. Proposed fix (awaiting user OK): make those sums error-tolerant (`SUMPRODUCT(...*IFERROR(range,0))`) so a transient feed flake counts the position as $0 instead of #VALUE!-ing the entire portfolio. Tradeoff: silent understatement of the erroring position until its feed recovers. See [[defi-wallet-tracking]].
Drive scope NOT authorized on gws (only Sheets+Gmail) → can't Drive-copy for backup; use in-workbook duplicateSheet instead.

## stocks-advisor P0 defects fixed (commit c36b5cf, 2026-07-07)
Session exposed that stocks-advisor emits confident EXIT verdicts on a broken evidence chain (NKE "no catalyst, dead money" EXIT while Narrative seat returned crypto noise + Smart-Money seat 403'd). Two P0 fixes, verified, committed:
- **P0-1 news pollution:** read-news `query()` searched the whole shared crypto+equity DB; `--source` only scoped FETCH not QUERY. Fixed: `query()` now honors a sources filter (BM25 + Jaccard both scoped) + Jaccard relevance floor 0.10 → no real hit returns `[]` + INSUFFICIENT_DATA note, not fuzzy crypto matches. NKE query: was 15 crypto-noise events → now []. 74/74 read-news tests pass.
- **P0-2 no sell-side gate:** every hierarchy gate only blocked BUYs. Added DATA-COVERAGE GATE to `bsc.md` CIO step: ≥2/5 seats INSUFFICIENT_DATA → cap verdict at HOLD/WATCH, print "DATA COVERAGE: N/5", name dark seats. Mirrors crypto-advisor UNCERTAIN→HOLD. `bridgewater.md` has same gap (noted, untouched).
**Lesson:** absence of evidence was being silently converted into evidence of "no catalyst." A trend-following EXIT on a cheap beaten-down name is the least-reliable signal precisely where the (broken) narrative+insider seats matter most. "24% below 200d" is TECHNICAL not fundamental — don't label trend signals as fundamentals.

## stocks-daily — 2026-07-07 (live re-run through fixed pipeline)
**Query:** Daily accumulation monitor, first run AFTER the P0 seat fixes (commit c36b5cf).
**Book:** $572,085 tracked (77 holdings, unchanged vs 07-06; delta = live-price drift from $584,579). Top-10 57.2%, crypto-beta 29.0%.
**Assets reviewed:** 14 full-panel (COIN NEM TSLA MRVL GOOG HOOD MSFT META AVGO QCOM CRCL KHC BMY BRK-B) + 63 one-line screened.
**THE key finding (honest, contradicts pre-run hypothesis):** the DATA-COVERAGE gate did NOT fire — openinsider was LIVE (200, not 403) for all 14, Sentiment live everywhere, so every name scored 4/5 or 5/5; only Narrative dark (11/14) = 1/5, below the 2/5 cap. The gate is correct and correctly stayed silent. What DID get proven: **P0-1 (narrative fix) works** — 11/14 returned INSUFFICIENT_DATA instead of crypto-polluted fuzzy matches; committee never fabricated a catalyst.
**Verdict changes — all driven by LIVE Form-4 insider data + position-size logic, not the gate:**
- BMY: ADD→HOLD | live insider pull came back NET-SELL (CFO ~$4.5M), not buy-heavy as the raw count first suggested. (Directly vindicates the session's NKE critique — this is exactly the insider check that was blind yesterday.)
- GOOG: TRIM→HOLD | cheap PEG holds it; 8/8 insider sells cap conviction, don't force a trim.
- AVGO: TRIM→HOLD | scorecard trim not position-weight-aware; 1.1% of book, overridden.
- CRCL: HOLD→TRIM(policy) | fundamentals a genuine EXIT (EPS -81%, cash-burning), crypto-beta policy caps at TRIM; weakest name in crypto sleeve.
**Swaps (proposed, no naked buys):**
- SELL NKE $4,969 + CMCSA $3,512 → BUY KHC ~$8,481 | KHC: CEO $4.99M open-market buy, 10.7% FCF yield, above 200d
- SELL ORCL $1,841 + JACK $1,254 + VSNT $224 + TRIM MRVL ~$5,000 → BUY BRK-B ~$8,319 | cheap+uptrend compounder
- COIN/CRCL trims stay INSIDE crypto sleeve (redeploy BTC), never fund non-crypto buys.
**Delta vs 07-06:** KHC/BRK-B still the two ADDs; BMY dropped out of ADDs on real insider selling; GOOG+AVGO softened trim→hold; NKE/CMCSA/ORCL still the funding EXITs (scorecard-driven, trend+fundamentals). Note NKE was NOT full-panel this run (one-line screened) — its EXIT is scorecard/trend only, still unconfirmed by narrative/insider.
**Telegram:** sent 3 parts (msg 8-10). **Report:** https://app.notion.com/p/396ac25eb49f817c9502e42c1ecb0783
